add_executable(${NAME}
        timer.c
        response.c
        functionality.c
        GGVTS.c
        uartInit.c
        ledFunctionality.c
        gsm.c
        stringoperations.c
        gps.c
        authenticator.c
        )

# Pull in our pico_stdlib which pulls in commonly used features
target_link_libraries(${NAME} pico_stdlib hardware_uart hardware_irq)

# create map/bin/hex file etc.
pico_add_extra_outputs(${NAME})